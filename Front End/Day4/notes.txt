CDN:
 (Content delivery network)
 :used to deliver the Content of the web pages from the surronding server to the client.

 Font awesome: it is used to add the icons in the web pages.
             : it is free and open source.
             : it is a library of icons

uiVerse:  it is a open source library of templates and components
         : it is used to design the web pages

css specificity rule 
            universal<element/pseudo-element<class/attribute/pseudo-class<id<inline

git:git is a version control system where we can store,modify,read the data
    : we can track the code

git init
git add .
git commit -m "text"
git remote add origin paste your github link here
git push origin master
        or
git push origin main